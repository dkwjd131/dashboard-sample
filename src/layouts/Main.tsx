import * as React from 'react';
import { main } from '../assets/styles/layout';

const Main = () => {
  return (
    <div css={main.container}>
      <div>main</div>
    </div>
  );
};

export default Main;
